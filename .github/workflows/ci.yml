name: CI

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  test-workflow:
    name: Test Snakemake Workflow
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Micromamba
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: environment.yaml
          environment-name: ocp-tool
          cache-environment: true
          cache-downloads: true
          generate-run-shell: true
      
      - name: Install Snakemake
        shell: micromamba-shell {0}
        run: |
          micromamba install -y -c conda-forge -c bioconda snakemake>=7.0
          
      - name: Lint Snakefile
        shell: micromamba-shell {0}
        run: |
          cd workflow
          snakemake --lint
      
      - name: Validate workflow configuration
        shell: micromamba-shell {0}
        run: |
          cd workflow
          python -c "import yaml; yaml.safe_load(open('config/config.yaml'))"
          echo "✓ Configuration file is valid YAML"
      
      - name: Check Python scripts syntax
        shell: micromamba-shell {0}
        run: |
          python -m py_compile ocp-tool.py
          python -m py_compile ocp_tool/*.py
          find workflow/scripts -name "*.py" -exec python -m py_compile {} +
          echo "✓ All Python scripts have valid syntax"
      
      - name: Dry run Snakemake workflow
        shell: micromamba-shell {0}
        run: |
          cd workflow
          # Create minimal test data structure
          mkdir -p ../input/{gaussian_grids_full,gaussian_grids_linear_reduced,openifs_input_default,runoff_map_default,fesom_mesh}
          mkdir -p ../output/{openifs_input_modified,oasis_mct3_input,runoff_map_modified,plots}
          
          # Test dry run with modified config for CI
          cat > test_config.yaml << EOF
          res_num: 159
          truncation_type: "linear"
          exp_name_oifs: "test"
          num_fields: 50
          grid_name_oce: "TEST"
          cavity: false
          interp_res: "r360x181"
          fesom_grid_file_path: "/tmp/test_mesh.nc"
          do_paleo: false
          manual_basin_removal: []
          EOF
          
          snakemake --dry-run --configfile test_config.yaml
          echo "✓ Snakemake workflow dry run successful"
      
      - name: Test import of ocp_tool modules
        shell: micromamba-shell {0}
        run: |
          python -c "
          import sys
          sys.path.insert(0, '.')
          from ocp_tool import ocp_tool
          print('✓ Successfully imported ocp_tool module')
          "

  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"
      
      - name: Install linting tools
        run: |
          pip install --upgrade pip
          pip install flake8 black isort mypy
      
      - name: Run flake8
        run: |
          # Stop the build if there are Python syntax errors or undefined names
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude=.git,__pycache__,build,dist
          # Exit-zero treats all errors as warnings
          flake8 . --count --exit-zero --max-complexity=15 --max-line-length=120 --statistics --exclude=.git,__pycache__,build,dist
      
      - name: Check code formatting with black
        run: |
          black --check --diff --exclude=.git . || echo "::warning::Code formatting issues found. Run 'black .' to fix."
      
      - name: Check import sorting with isort
        run: |
          isort --check-only --diff . || echo "::warning::Import sorting issues found. Run 'isort .' to fix."

  notebook-tests:
    name: Test Jupyter Notebooks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Micromamba
        uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: environment.yaml
          environment-name: ocp-tool
          cache-environment: true
          generate-run-shell: true
      
      - name: Validate notebook structure
        shell: micromamba-shell {0}
        run: |
          pip install nbformat
          python -c "
          import nbformat
          import json
          
          nb = nbformat.read('ocp_tool.ipynb', as_version=4)
          print(f'✓ Notebook has {len(nb.cells)} cells')
          print(f'✓ Notebook format version: {nb.nbformat}.{nb.nbformat_minor}')
          
          # Check for basic structure
          code_cells = [c for c in nb.cells if c.cell_type == 'code']
          print(f'✓ Found {len(code_cells)} code cells')
          "
      
      - name: Check notebook for output clearing
        shell: micromamba-shell {0}
        run: |
          python -c "
          import nbformat
          import sys
          
          nb = nbformat.read('ocp_tool.ipynb', as_version=4)
          has_outputs = False
          
          for cell in nb.cells:
              if cell.cell_type == 'code' and cell.outputs:
                  has_outputs = True
                  break
          
          if has_outputs:
              print('::warning::Notebook contains outputs. Consider clearing before commit.')
          else:
              print('✓ Notebook outputs are cleared')
          "

  documentation:
    name: Documentation Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Check README files
        run: |
          for readme in README.md workflow/README.md; do
            if [ -f "$readme" ]; then
              echo "✓ Found $readme"
            else
              echo "::error::Missing $readme"
              exit 1
            fi
          done
      
      - name: Check for broken links in documentation
        uses: gaurav-nelson/github-action-markdown-link-check@v1
        with:
          use-quiet-mode: 'yes'
          config-file: '.github/markdown-link-check-config.json'
        continue-on-error: true

  security-scan:
    name: Security Scanning
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Run Trivy security scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
        continue-on-error: true
      
      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v2
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
        continue-on-error: true